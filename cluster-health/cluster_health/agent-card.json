{
  "name": "cluster_health",
  "description": "Kubernetes cluster health monitoring agent. Can check pod status, node health, deployments, events, and resource usage.",
  "url": "http://cluster-health-agent.kagent:8080",
  "version": "0.0.1",
  "capabilities": {
    "streaming": true
  },
  "defaultInputModes": ["text"],
  "defaultOutputModes": ["text"],
  "skills": [
    {
      "id": "cluster_health",
      "name": "Cluster Health",
      "description": "Monitor Kubernetes cluster health including pods, nodes, deployments, events, and resource usage",
      "tags": ["kubernetes", "monitoring", "health", "sre"]
    }
  ]
}
